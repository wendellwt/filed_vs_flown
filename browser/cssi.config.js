
const path = require("path");

module.exports = {

    // Note: the number here is the one assigned by the 1st 'rconnect deploy'
    publicPath: "/",

    outputDir: "../service/code/",

    "transpileDependencies": [
        "buefy"
      ]
}

